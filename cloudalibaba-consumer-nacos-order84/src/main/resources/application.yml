server:
  port: 84

spring:
  application:
    name: nacos-order-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    sentinel:
      transport:
        dashboard: localhost:8080
        port: 8719

# 自定义服务地址名
service-url:
  naocs-user-service: http://nacos-payment-provider

feign:
  sentinel:
    enabled: true
